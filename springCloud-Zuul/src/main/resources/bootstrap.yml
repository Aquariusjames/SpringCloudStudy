spring:
  application:
    name: springCloud-zuul
  cloud:
    config:
      uri: http://user:password123@localhost:8031/
server:
  port: 8021
eureka:
  client:
    service-url:
      defaultZone: http://user:password@peer1:8761/eureka/
  instance:
    prefer-ip-address: true